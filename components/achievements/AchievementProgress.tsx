/** AchievementProgress — overall progress summary (X/total unlocked). TODO: implement from Stitch design. */
export function AchievementProgress() {
  return <div className="rpg-panel p-6">{/* TODO */}</div>
}
