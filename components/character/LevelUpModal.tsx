"use client"

interface LevelUpModalProps {
  newLevel: number
  open: boolean
  onClose: () => void
}

/** LevelUpModal — celebration overlay when player levels up. TODO: implement from Stitch design. */
export function LevelUpModal({ open: _open, onClose: _onClose, newLevel: _newLevel }: LevelUpModalProps) {
  return null /* TODO */
}
