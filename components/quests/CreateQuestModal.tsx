"use client"

interface CreateQuestModalProps {
  open: boolean
  onClose: () => void
}

/** CreateQuestModal — modal for creating a new quest. TODO: implement from Stitch design. */
export function CreateQuestModal({ open: _open, onClose: _onClose }: CreateQuestModalProps) {
  return null /* TODO */
}
